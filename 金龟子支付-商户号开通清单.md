# 金龟子微信支付开通清单（个体工商户 + 商户号）

> 决策：用户选择正规合规路径（个人收款码无法 API 接入金龟子现有支付）。
> 代码侧已 100% 就绪：provider=wechatpay_v2（Native 扫码），仅需 mchid + appid + v2_key，无需证书/v3 key。
> 线上 config 的 `notify_base_url` 已改为 https://www.kinguizi.top，sandbox=false 已就绪。

---

## 一、你需要办的事（按顺序）

### 1. 办个体工商户营业执照
- 各地政务网 / 市场监管局 / 微信"微信支付商家助手"小程序均可办，1–3 天，成本极低（多数地区免费或几十元）。
- 经营范围写清楚含"互联网信息服务 / 软件开发 / 技术服务"之类，便于过审。
- 个体户可用**法人个人储蓄卡**作为结算账户（无需对公账户）。

### 2. 申请微信支付商户号（绑定该个体户）
- 入口：微信"微信支付商家助手"小程序 → 成为商家；或 pay.weixin.qq.com。
- 提交：营业执照 + 法人身份证 + 法人个人储蓄卡 + 门店/经营信息。
- 审核 1–3 工作日，通过后拿到 **商户号（mchid，10 位数字）**。

### 3. 开通「Native 支付」产品（关键！）
- 登录商户平台 → 产品中心 → 申请 **Native 支付（扫码支付）**。
- ⚠️ 不申请这个产品，真充值会返回 `NOAUTH`。金龟子代码正是 Native。

### 4. 准备 appid（这一步最容易踩坑）
微信支付 v2 Native 下单必须传 appid，且 appid 需是**已认证的主体**并在商户平台授权绑定。二选一：
- **方案 A（推荐，最稳）**：注册一个**微信公众平台服务号**并微信认证（300 元/年），用其 appid。
- **方案 B（省认证费）**：把你已有的微信小程序 `ardot-design-assistant` 从"个人主体"迁移为"个体工商户"（用同一执照），迁移后用 `ardot` 的 appid 绑定商户号。
- 拿到 appid 后，在商户平台「APPID 授权管理」里把该 appid 与商户号绑定。

### 5. 设置 APIv2 密钥
- 商户平台 → API 安全 → 设置 **APIv2 密钥**（32 位，自己设，记好）。这就是 `v2_key`。

---

## 二、办好后，把这 3 个值发我

| 字段 | 说明 | 来源 |
|------|------|------|
| `mchid` | 商户号 | 步骤 2 审核通过邮件/公众号通知 |
| `appid` | 已认证公众号(服务号)或个体户小程序 appid | 步骤 4 |
| `v2_key` | APIv2 密钥(32位) | 步骤 5 |

> 安全提示：建议通过私密渠道发我，或我直接帮你写成服务器上的环境变量文件（不落明文 config）。

---

## 三、我拿到后会做的事
1. 在服务器用环境变量注入 `WECHATPAY_MCHID / WECHATPAY_APPID / WECHATPAY_V2_KEY`（代码已支持，不写进明文 config）。
2. 重启 `goldarena` 服务使配置生效。
3. 用沙箱切换真支付，端到端验证：下单 → 生成微信扫码 → 真支付 → 回调自动入账（游戏币充值 + 选拔赛缴费报名同时打通）。
4. 验证通过后把 `sandbox=false` 固化，完成上线。

---

## 四、不需要做的事
- 不需要 APIv3 密钥、不需要商户证书（serial_no / private_key / api_v3_key 都留空）。
- 不需要二清/聚合支付等灰色通道（合规风险高，金龟子未采用）。
